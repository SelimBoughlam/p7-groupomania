import React from "react";
import logo from "./logo.svg";

const Logo = () => {
  return (
    <div className="logo">
      <img src={logo} alt="logo groupomania" />
    </div>
  );
};

export default Logo;
